apiVersion: v1
kind: Service
metadata:
  name: {{ include "avuruops.fullname" . }}-hub
  labels:
    {{- include "avuruops.labels" . | nindent 4 }}
    app.kubernetes.io/component: hub
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "avuruops.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hub
  ports:
    - name: http
      port: 80
      targetPort: http
