apiVersion: v1
kind: Service
metadata:
  name: {{ include "avuruobs.fullname" . }}-hub
  labels:
    {{- include "avuruobs.labels" . | nindent 4 }}
    app.kubernetes.io/component: hub
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "avuruobs.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hub
  ports:
    - name: http
      port: 80
      targetPort: http
